支持配置监听 IP 与端口：新增 -bind 参数

- 新增 -bind flag：显式指定监听地址（IP 或主机名，支持 IPv6 如 ::）；
  留空时回退 IRIX_NODE_BIND_ALL 环境变量（=1 则 0.0.0.0），否则默认 127.0.0.1
- 端口校验 1-65535；监听地址改用 net.JoinHostPort 拼接（IPv6 安全）
- 票据直连地址仍随 BindHost/Port 生成（publicAddr 已支持 0.0.0.0/:: 对外 IP 探测）
- resolveBind 提取为可测函数 + TestResolveBind（flag 优先/环境变量回退/IPv6/去空白）
- 更新 README/AGENTS/CLAUDE 的参数表与命令示例
