新增零侵入遥测：/api/metrics（Prometheus 格式）+ /debug/pprof

- telemetry.go：业务 handler 零改动。HTTP 计数（请求总数/状态码分类/响应字节/
  延迟直方图）全部在 telemetryMiddleware 中间件层完成（仅包装一层）；
  Go 运行时指标直接读标准库 runtime/metrics（gauge 与 histogram 两类，防御式
  兼容不同 Go 版本 Counts/Buckets 布局差异）；实例统计读已有状态。
- /api/metrics：Prometheus 文本格式，认证与其余 API 一致，可被 Prometheus 直接抓取。
- /debug/pprof/*：标准库 pprof（Index/Cmdline/Profile/Symbol/Trace），带认证，
  支持 CPU/内存剖析、goroutine、trace。
- 接入：main.go 中间件链加一层、RegisterRoutes 加一个注册调用，共 2 行。
- 测试：qa_telemetry_test.go 覆盖计数器输出、pprof 端点、countingWriter、指标名转换。
