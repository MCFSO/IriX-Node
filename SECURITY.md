# 安全策略（Security Policy）

本文件描述 IriX Node 守护进程的安全支持范围、漏洞报告方式，以及部署方应遵循的
安全基线。**IriX Node 是一个本地节点守护进程**，默认仅监听 `127.0.0.1`，拥有对
本机实例进程、文件与运行环境的高度控制力，部署方对监听范围与凭据保密负有首要责任。

## 支持的版本

我们仅对 `main` 分支的最新提交提供安全修复。安全补丁通过常规提交合入 `main` 并
随后续发布分发，不会单独维护历史版本的长周期安全分支。

| 分支 / 版本 | 安全支持 |
| --- | --- |
| `main`（最新） | ✅ 接收并修复安全漏洞 |
| 已发布的旧版本 | ❌ 不提供单独的安全补丁，请升级到最新 `main` |

## 报告漏洞（Reporting a Vulnerability）

### 首选渠道：私密报告

请**不要**通过公开 Issue 报告安全漏洞。优先使用以下私密渠道之一：

- **GitHub 私密漏洞报告（Private Vulnerability Reporting）**：在本仓库的
  `Security → Report a vulnerability` 中提交（若仓库已开启该功能）。
- **加密邮件**：若提供安全联系邮箱，请用其 PGP 公钥加密后发送。公钥见仓库
  `SECURITY.md` 或 `.github` 目录下的最新公告。

### 报告内容

请尽量包含以下信息，以便复现与定级：

1. 受影响版本（commit 哈希或发布版本）与运行平台（Windows / Linux / BSD）。
2. 漏洞类型（如认证绕过、权限提升、路径越界、SSRF、敏感信息泄露等）。
3. 复现步骤、PoC（如有）及预期 / 实际影响。
4. 已启用的相关配置（`-apikey` / `-tls-mode` / `-vault` / `-transfer-allow-cidr`
   等），以及是否监听公网 `0.0.0.0`。

### 处理流程与时限

- 收到报告后，维护者将在 **3 个工作日内**确认受理并分配跟进人。
- 确认有效后，我们将与报告者协商披露时间，默认遵循 **90 天协同披露**原则；
  若已有可用的修复，可提前公开。
- 严重漏洞（可远程未授权接管节点）在修复就绪前，我们会通过私密渠道向受影响部署方
  提供临时缓解建议。

### 致谢

修复合并后，经报告者同意，我们会在发布说明或本文件末尾的致谢列表中署名。

## 安全基线（部署方责任）

IriX Node 的部分安全性依赖于部署方式。下列为**强制或强烈建议**的部署基线：

### 1. 监听范围与凭据

- 默认仅监听 `127.0.0.1`；**不要**将无强凭据的节点暴露到 `0.0.0.0` / 公网。
- 局域网或远程访问时，务必设置强 `-apikey`（`?apikey=` 或 `X-Api-Key`），
  并通过防火墙限制来源 IP。
- 使用配对码模式时，配对码仅首次启动显示一次：`auth.hash` 为 SHA-256 哈希，
  泄露文件不会直接泄露配对码，但**删除 `auth.hash` 并重启会重置配对码**——请妥善保管。

### 2. 首次登录强制改密

- 配对码登录即 **root 管理员**；首次登录会强制要求改密，此后配对码不再用于登录。
- 请为所有账户设置高复杂度密码，并避免长期复用 root 凭据。

### 3. 传输加密（TLS）

- TLS 默认关闭，由部署方显式开启（`-tls-mode auto` 自签 / `manual` 正式证书）。
- 启用加密保险库 Vault（`-vault`）时**强制要求 TLS 已开启**，否则拒绝启动。
- 等保二级或任何远程管理场景，必须开启 TLS 以防凭据与数据在传输中被窃听。

### 4. 权限最小化

- 账户体系支持对**每个 API 端点**独立开关权限（按模块分组）。
- 遵循最小权限原则：仅为账户开启其工作所需的端点，禁用不必要的整组权限。

### 5. 集群直传信任边界

- 集群拉取（`POST /api/cluster/transfer`）默认拒绝环回、链路本地、本机与
  RFC1918 内网地址（防认证后 SSRF）。
- 仅在确属自己管辖的网段时，才通过 `-transfer-allow-cidr` 显式放行；
  该配置即信任边界，误配会扩大攻击面。

### 6. 审计与备份

- 审计日志（`-audit-log`，默认开启）会记录每请求的时间 / IP / 方法 / 路径 /
  状态码 / 敏感字段打码；请保留并定期归档到 `{data}/backup/audit/`。
- 启用 Vault 时，定期 `POST /api/vault/backup` 导出加密备份包；
  **恢复令牌一次性显示，丢失即数据不可恢复**，请物理保管。

## 已知残余风险（如实披露）

以下内容为当前架构的固有或 best-effort 限制，供部署方评估：

- **解锁会话期间进程被攻破**：会话内数据主密钥驻留内存，架构 A 的固有残余风险；
  缓解措施为空闲超时（默认 30 分钟）、手动锁定与审计留痕。
- **离线暴力破解**：密码强度即离线安全上限；采用 PBKDF2 高迭代 + 复杂度策略缓解。
- **剩余信息保护**：Go 运行时无法保证敏感数据物理擦除（GC / swap）；
  实现为显式清零 + 锁定销毁（best-effort），不声称可对抗介质取证。

## 安全相关文档

- `docs/accounts-design.md`：账户管理、认证通道、端点权限模型。
- `docs/vault-design.md`：加密保险库 Vault 设计（等保二级目标）。
- `docs/compliance-l2.md`：等保二级自查表与技术控制项落地状态。
- `README.md`「安全说明」：路径越界防护、cwd 限制、集群直传默认拒绝策略。

---

## 致谢（Hall of Fame）

_暂无。欢迎负责任地披露漏洞，经授权后在此署名。_
