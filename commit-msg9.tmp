修复「无法创建 Jail」两大真实根因（SSH 实测验证于 FreeBSD 15.1 + bastille 1.4.4）

根因 1：客户端把显示标签当 release 传入（"15.0-RELEASE:15.0-RELEASE"），
导致 release 目录查找失败。服务端 normalizeRelease 剥离冒号后缀取发行版名。

根因 2：bastille 拒绝纯数字 jail 名（实测 "Jail names may not contain only
digits"，用户实例名 "1145"）。客户端校验允许纯数字，服务端以 400 拦截并给
中文提示「请至少包含一个字母（如 mc1145）」。

附带修复：
- jail 列表 release 推断优先解析 jail.conf 的 osrelease 行（bastille 1.4+ 的
  thin jail root 不再是符号链接，旧 Readlink 方式拿不到 release）
- 实测确认：releases sizeBytes 显示 3.7GB 真实大小、创建成功、status "Up"
- 部署经验：FreeBSD 运行中的二进制文件不可覆盖，scp 需先停进程
